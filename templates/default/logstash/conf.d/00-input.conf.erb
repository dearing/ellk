input {
  lumberjack {
    port => "<%= @options['port'] %>"
    type => "logs"
    ssl_certificate => "<%= @options['crt_location'] %>"
    ssl_key => "<%= @options['key_location'] %>"
  }
}
